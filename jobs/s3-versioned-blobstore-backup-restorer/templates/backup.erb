#!/usr/bin/env bash

set -eu

<% if p('enabled') %>
  <% if p("bpm.enabled") %>
/var/vcap/jobs/bpm/bin/bpm run s3-versioned-blobstore-backup-restorer \
  -p backup \
  -v "${BBR_ARTIFACT_DIRECTORY%/}:writable"
  <% else %>
/var/vcap/jobs/s3-versioned-blobstore-backup-restorer/bin/bbr/run_backup
  <% end %>
<% end %>
